Web Studio — платформа услуг и продвижения
Стек: Angular 14, Router, RxJS, HTTPClient, Material, Lazy Loading
— Реализована структура платформы: услуги, кейсы, блог, отзывы
— Настроино API-взаимодействие и динамический вывод контент

## 🚀 Локальный запуск проекта

### 1. Предварительные требования

Перед запуском убедитесь, что у вас установлено:

- **Node.js** (LTS-версия, например 18.x или 20.x)
- **npm** (идёт вместе с Node.js)
- **Angular CLI** (глобально, при необходимости)
- npm install -g @angular/cli

  Установка зависимостей
- npm install

Запуск проекта в режиме разработки
# Вариант 1 — через Angular CLI
ng serve

# Вариант 2 — через npm-скрипт
npm run start

